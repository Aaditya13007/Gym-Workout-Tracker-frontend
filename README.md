# Gym-Workout-Tracker-frontend
 Gym Workout Tracker frontend is built with HTML, CSS, and JavaScript. It includes a responsive Home page with a navbar, workout summary, search/filter controls, and dynamically rendered workout cards. It also has an Add Workout page with a validated form that sends new workout data to the backend API.
